- if @user.galleries.present?
  .large-12.columns.panel
    %h3.small.subheader Galleries
    %ul.small-block-grid-2.medium-block-grid-3.large-block-grid.4
      - @user.galleries.each do |gallery|
        %li
          = image_tag Photo.first.image.url(:thumb), class: "th gallery_photo"
          %h5
            = link_to gallery.title, gallery_path(gallery)



-# - if current_user.ads.present?
-#   .large-12.columns.panel
-#     %h3.small.subheader Open Projects
-#     - current_user.ads.each do |ad|
-#       .row
-#         .large-4.columns
-#           = image_tag ad.project_photo.url(:thumb), class: "th"
-#         .large-4.columns
-#           %h4
-#             = link_to ad.title, ad_path(ad)
-#           %p
-#             = "Date Posted: "
-#             = "#{ad.created_at.strftime("%m/%d/%Y")}"
-#           %h5 Tags
-#           - ad.tags.each do |tag|
-#             = link_to tag.name
-#         .large-4.columns
-#           %p= ad.description

-# .row
-#   .columns.small-12.medium-6.large-10
-#     %ul.small-block-grid-3
-#       - if @user.galleries.present?
-#         - @user.galleries.each do |gallery|
-#           %li
-#             = image_tag Photo.first.image.url(:thumb), class: "th gallery_photo"
-#             %h5
-#               = link_to gallery.title, gallery_path(gallery)
-#       - else
-#         %p You haven't created any galleries yet!
